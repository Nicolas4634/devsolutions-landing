services:
  - type: web
    name: devsolutions-landing
    runtime: static
    plan: free
    buildCommand: npm ci && npm run build
    staticPublishPath: out
    envVars:
      - key: NODE_VERSION
        value: "20"
